Options -Indexes
RewriteEngine On

# If the request is for a real file or folder, serve it directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Try appending .php to the URL
RewriteRule ^([^\.]+)$ $1.php [NC,L]